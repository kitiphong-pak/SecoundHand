// ตัวเลขทั้งหมดของ flow ออเดอร์แบบนัดเจอ รวมไว้ที่เดียว
//
// ค่าพวกนี้ "เดาไว้ก่อน" ทั้งหมด ยังไม่มีข้อมูลผู้ใช้จริงมายืนยันสักตัว เก็บไว้ที่เดียวเพื่อให้แก้ทีเดียว
// จบเมื่อเห็นพฤติกรรมจริง และเพื่อให้รู้ว่ามีตัวเลขอะไรที่ต้องตรวจสอบบ้าง — อย่ากระจายไปฝังในโค้ด

/** จองไว้ได้นานแค่ไหนโดยไม่มีใครขยับ ก่อนระบบยกเลิกและปล่อยสินค้ากลับไปขายต่อ */
export const RESERVATION_HOLD_MS = 24 * 60 * 60 * 1000; // 24 ชม.

/**
 * คนหนึ่งจองค้างไว้พร้อมกันได้กี่ชิ้น
 *
 * การจองไม่มีค่าใช้จ่ายและยกเลิกฟรี ถ้าไม่จำกัด คนเดียวกดจองล็อกสินค้าทั้งหมวดได้ทีละ 24 ชม.
 */
export const MAX_OPEN_RESERVATIONS_PER_BUYER = 3;

/** นัดล่วงหน้าได้ไกลสุดเท่าไร — กันพิมพ์ปีผิดแล้วสินค้าถูกจองค้างข้ามปี */
export const MEETUP_MAX_AHEAD_MS = 30 * 24 * 60 * 60 * 1000; // 30 วัน

/** ความยาวสูงสุดของสถานที่นัด — พอสำหรับ "หน้า BTS อโศก ทางออก 3" แต่ไม่ให้ยัดเรียงความลงแชท */
export const MEETUP_PLACE_MAX_LENGTH = 120;

/** ยกเลิกก่อนเวลานัดน้อยกว่านี้ถือว่ากระชั้นชิด (Phase 2 เอาไปคิดคะแนน) */
export const LATE_CANCEL_WINDOW_MS = 60 * 60 * 1000; // 1 ชม.

/** หลังเวลานัด อีกฝ่ายเงียบนานแค่ไหน ระบบถึงปิดดีลให้จากการยืนยันฝ่ายเดียว */
export const AUTO_COMPLETE_AFTER_MEETUP_MS = 48 * 60 * 60 * 1000; // 48 ชม.

/** แจ้งว่าอีกฝ่ายไม่มาได้หลังเลยเวลานัดไปแล้วเท่าไร (Phase 2) */
export const NO_SHOW_REPORT_DELAY_MS = 30 * 60 * 1000; // 30 นาที

/** ถูกแจ้งว่าไม่มาแล้วโต้แย้งได้ภายในกี่ชั่วโมง (Phase 2) */
export const NO_SHOW_CONTEST_WINDOW_MS = 24 * 60 * 60 * 1000; // 24 ชม.

/** เปิดข้อพิพาทหลังปิดดีลได้ภายในกี่วัน — ย้ายมาจาก orderTiming.ts ของ flow เดิม */
export const DISPUTE_GRACE_MS = 2 * 24 * 60 * 60 * 1000; // 2 วัน
